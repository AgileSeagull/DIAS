# ============================================================================
# DIAS - Disaster Information & Alert System
# Environment Configuration
# ============================================================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dias
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# JWT Configuration (for user authentication)
# Generate a strong random secret: openssl rand -base64 32
JWT_SECRET=change_this_to_a_random_secret_key_in_production
JWT_EXPIRES_IN=7d

# AWS Configuration (for SNS email alerts)
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Optional: Geocoding API (for better location extraction)
# Get free API key from: https://opencagedata.com/
# GEOCODING_API_KEY=your_geocoding_api_key_here

# ============================================================================
# Setup Instructions:
# ============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update all values with your actual credentials
# 3. Never commit .env to git (it's in .gitignore)
# 4. For AWS setup, see: https://console.aws.amazon.com/iam
# 5. For JWT secret, run: openssl rand -base64 32
# ============================================================================
